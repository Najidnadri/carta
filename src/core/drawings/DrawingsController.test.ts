// `normalizeDrawingDefaults` tests live in `./normalize.test.ts` (Cycle B.3).
// Controller-level tests (multi-select, pinch-cancels-drag, tryClaimLongPress,
// getDevHooks) will land in this file as the rest of the cycle ships.
import { describe, it } from "vitest";

describe("DrawingsController — Cycle B.3", () => {
  it.skip("placeholder — see ./normalize.test.ts", () => {
    // intentionally empty
  });
});
